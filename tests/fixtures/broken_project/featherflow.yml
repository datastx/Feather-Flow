name: broken_project
version: "1.0.0"

model_paths: ["models"]
target_path: "target"

materialization: view
schema: analytics
dialect: duckdb

database:
  type: duckdb
  path: ":memory:"

external_tables:
  - raw.orders
  - raw.customers
