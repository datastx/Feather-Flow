name: sample_project
version: "1.0.0"

model_paths: ["models"]
seed_paths: ["seeds"]
source_paths: ["sources"]
macro_paths: ["macros"]
target_path: "target"

materialization: view
schema: analytics
dialect: duckdb

database:
  type: duckdb
  path: ":memory:"

vars:
  start_date: "2024-01-01"
  environment: dev
