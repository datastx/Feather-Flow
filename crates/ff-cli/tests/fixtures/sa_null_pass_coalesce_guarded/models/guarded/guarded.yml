version: 1
name: guarded
description: "LEFT JOIN with COALESCE guards — no A010"
columns:
  - name: amount
    data_type: DECIMAL(10,2)
  - name: name
    data_type: VARCHAR
  - name: email
    data_type: VARCHAR
