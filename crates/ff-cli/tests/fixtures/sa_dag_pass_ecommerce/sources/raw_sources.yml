kind: sources
name: raw_ecommerce
schema: main
tables:
  - name: raw_orders
    columns:
      - name: id
        type: INT
      - name: customer_id
        type: INT
      - name: amount
        type: "DECIMAL(10,2)"
      - name: status
        type: VARCHAR
      - name: created_at
        type: TIMESTAMP
  - name: raw_customers
    columns:
      - name: id
        type: INT
      - name: name
        type: VARCHAR
      - name: email
        type: VARCHAR
  - name: raw_products
    columns:
      - name: id
        type: INT
      - name: name
        type: VARCHAR
      - name: price
        type: "DECIMAL(10,2)"
      - name: category
        type: VARCHAR
