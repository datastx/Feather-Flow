kind: sql
version: 1
name: stg_customers
description: "Staging customers model"
columns:
  - name: id
    data_type: INTEGER
    tests: [unique, not_null]
  - name: name
    data_type: VARCHAR
    tests: [not_null]
  - name: email
    data_type: VARCHAR
