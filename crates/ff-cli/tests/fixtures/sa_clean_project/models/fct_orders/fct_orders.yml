version: 1
name: fct_orders
description: "Fact orders model"
columns:
  - name: id
    data_type: INTEGER
    tests: [unique, not_null]
  - name: amount
    data_type: DECIMAL(10,2)
  - name: status
    data_type: VARCHAR
  - name: customer_name
    data_type: VARCHAR
  - name: created_at
    data_type: TIMESTAMP
    tests: [not_null]
