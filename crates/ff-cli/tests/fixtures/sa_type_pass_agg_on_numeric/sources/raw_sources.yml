kind: sources
name: raw_ecommerce
schema: main
tables:
  - name: raw_data
    columns:
      - name: id
        type: INTEGER
      - name: amount
        type: DECIMAL(10,2)
      - name: name
        type: VARCHAR
