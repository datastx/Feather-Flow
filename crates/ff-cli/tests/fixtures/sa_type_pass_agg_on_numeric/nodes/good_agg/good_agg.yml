kind: sql
version: 1
name: good_agg
description: "SUM on numeric, COUNT/MIN/MAX on string — no A004"
columns:
  - name: total_amount
    data_type: DECIMAL(10,2)
  - name: name_count
    data_type: BIGINT
  - name: first_name
    data_type: VARCHAR
  - name: last_name
    data_type: VARCHAR
