kind: sources
version: 1
name: raw_ecommerce
description: "Raw source data definitions"
schema: main
tables:
  - name: raw_data
    description: "Raw data data"
    columns:
      - name: id
        description: "Unique identifier for the record"
        type: INTEGER
      - name: amount
        description: "Monetary amount in dollars"
        type: DECIMAL(10,2)
      - name: name
        description: "Name of the entity"
        type: VARCHAR
