kind: sql
version: 1
name: fct
description: "Fact model consuming only id and name from stg"
columns:
  - name: id
    description: "Unique identifier for the record"
    data_type: INTEGER
  - name: name
    description: "Name of the entity"
    data_type: VARCHAR
