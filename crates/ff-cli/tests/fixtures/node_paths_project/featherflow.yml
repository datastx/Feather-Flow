name: node_paths_project
version: "1.0.0"

node_paths: ["nodes"]
macro_paths: ["macros"]
target_path: "target"

materialization: view
schema: analytics
dialect: duckdb

database:
  type: duckdb
  path: ":memory:"
  name: main

vars:
  start_date: "2024-01-01"
