version: 1
kind: sql

description: "Staged orders from raw source"
tags:
  - staging

columns:
  - name: order_id
    type: INTEGER
    description: "Unique order identifier"
    tests:
      - unique
      - not_null

  - name: customer_id
    type: INTEGER
    tests:
      - not_null

  - name: amount
    type: DECIMAL(10,2)
    tests:
      - not_null
