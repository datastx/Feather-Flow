kind: sql
version: 1
name: range_join
description: "Non-equi join with > operator — should trigger A033"
columns:
  - name: id
    description: "Unique identifier for the record"
    data_type: INTEGER
