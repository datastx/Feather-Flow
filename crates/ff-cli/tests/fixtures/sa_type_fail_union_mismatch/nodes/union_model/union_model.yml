kind: sql
version: 1
name: union_model
description: "Union of INT and VARCHAR — should trigger A002"
columns:
  - name: id
    description: "Unique identifier for the record"
    data_type: INTEGER
