kind: sources
version: 1
name: raw_ecommerce
description: "Raw source data definitions"
schema: main
tables:
  - name: raw_ints
    description: "Raw ints data"
    columns:
      - name: id
        description: "Unique identifier for the record"
        type: INTEGER
  - name: raw_strings
    description: "Raw strings data"
    columns:
      - name: name
        description: "Name of the entity"
        type: VARCHAR
