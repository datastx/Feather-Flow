kind: sql
materialized: view
version: 1

description: "A valid model that should compile even when siblings fail"

columns:
  - name: id
    description: "Unique identifier for the record"
    type: INTEGER
  - name: status
    description: "Current status of the record"
    type: VARCHAR
