kind: sources
version: 1

name: raw_data
description: "Raw data source"
schema: main

tables:
  - name: raw_events
    description: "Raw events"
    columns:
      - name: id
        type: INTEGER
      - name: status
        type: VARCHAR
