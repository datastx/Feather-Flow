version: 1
name: agg_model
description: "Aggregate function stubs: COUNT, SUM, AVG, MIN, MAX"
columns:
  - name: cnt
    data_type: BIGINT
  - name: total
    data_type: DECIMAL(10,2)
  - name: avg_amt
    data_type: DECIMAL(10,2)
  - name: min_amt
    data_type: DECIMAL(10,2)
  - name: max_amt
    data_type: DECIMAL(10,2)
