version: 1
name: contradiction
description: "LEFT JOIN makes name nullable, but YAML declares NOT NULL — should trigger A011"
columns:
  - name: id
    data_type: INTEGER
  - name: name
    data_type: VARCHAR
    constraints: [not_null]
