kind: sql
version: 1
name: joined
description: "LEFT JOIN with unguarded nullable columns — should trigger A010"
columns:
  - name: id
    description: "Unique identifier for the record"
    data_type: INTEGER
  - name: name
    description: "Name of the entity"
    data_type: VARCHAR
  - name: email
    description: "Email address"
    data_type: VARCHAR
