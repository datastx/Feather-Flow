version: 1
name: joined
description: "LEFT JOIN with unguarded nullable columns — should trigger A010"
columns:
  - name: id
    data_type: INTEGER
  - name: name
    data_type: VARCHAR
  - name: email
    data_type: VARCHAR
