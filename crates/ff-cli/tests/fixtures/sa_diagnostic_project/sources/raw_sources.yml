kind: sources
name: raw_data
schema: main
tables:
  - name: raw_orders
    columns:
      - name: id
        data_type: INTEGER
      - name: amount
        data_type: DECIMAL(10,2)
      - name: status
        data_type: VARCHAR
