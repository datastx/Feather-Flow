kind: sources
version: 1
name: raw_data
schema: main
tables:
  - name: raw_orders
    columns:
      - name: id
        type: INTEGER
      - name: amount
        type: DECIMAL(10,2)
      - name: status
        type: VARCHAR
